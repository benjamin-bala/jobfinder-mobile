import { metrics } from './metrics';
import { colors } from './colors';
import { fontsize, fontweight, fonttype } from './fonts';

const font = { fontsize, fontweight, fonttype };

export { metrics, colors, font };
